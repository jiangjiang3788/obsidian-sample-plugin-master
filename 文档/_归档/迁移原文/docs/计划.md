| 阶段         | 优先级       | 修改类型                                           | 进度（最新）                       | 原因说明（简）                                                                                                                                         | 涉及文件（主）                                                                                    | 危险性 | 验收                                                           | 建议测试命令                                                                                                    |
| ---------- | --------- | ---------------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | --- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| Phase0     | P0        | Typecheck 收敛（87→0）工程止血                         | 🔴 当前 1 个 TS error           | `BlockItem.tsx` 传 `app` 给 `MarkdownContent`，但 props 无此字段，`npm run typecheck` 报错                                                                 | `tsconfig*.json` / `package.json` / `src/shared/ui/items/BlockItem.tsx`                    | 🔥  | `npm run typecheck` = 0 errors                               | **`npm run typecheck`**                                                                                   |
| Phase0     | P0        | 最小测试框架（Jest + smoke≥3）                         | 🟡 框架 OK，但 1 个单测失败           | Jest 配置正常，6 个 unit suite 中 5 个 PASS；`legacyCalloutBlock.test.ts` 挂（content 为空，没 `- [x]`）                                                        | `test/configs/jest.config.js` / `test/unit/*`                                              | 🔥  | `npm run test:unit` 全 PASS（smoke ≥3）                         | **`npm run test:unit`**（重点看 `legacyCalloutBlock`）                                                         |
| Phase0     | P1        | 解析语义止血：Task 主题字段归位 + Theme 显式优先                | 🟡 主线 OK，legacy 场景待修         | parser/DataStore/Theme 主线没爆，但 legacy callout 语义在 `legacyCalloutBlock.test.ts` 失败                                                                | `src/core/utils/parser.ts` / `src/core/services/DataStore.ts` / `src/core/types/schema.ts` | 🔥  | 主题不混 tags；Theme 优先级一致；相关单测全绿                                 | 还是 **`npm run test:unit`**，修好 legacy 用例；必要时加几条 parser 单测                                                  |
| Phase0     | P2        | 生命周期收口：统一 dispose + 卸载安全                       | 🟡 结构/单测已在，缺运行态证据            | `disposables.test.ts`、`lifecycleState.test.ts` PASS，`events-boundary:gate` ✅；但没真实 unload/reload 压测/日志                                           | `ServiceManager` / `Disposables`                                                           | 💥  | reload/unload 无遗留订阅/定时器（日志/计数/手工验证）                          | 单测：`npm run test:unit`（确认相关用例仍绿）＋ 建议加 1 条小 E2E/脚本做 unload/reload 自测                                       |
| Phase0     | P2        | 共享层去平台化（Notice 等）                              | 🟡 门禁已绿，业务抽象待确认              | `obsidian-leak:gate` 等门禁 PASS，说明 shared/core 不再直接 import obsidian；但 Notice 是否全部走 UiPort 还需人工确认                                                  | `shared/*` / `UiPort` / `scripts/obsidian-leak-gate*`                                      | 🔥  | shared/core 无非法 obsidian import；Notice 全走 UiPort；相关 gate 全绿  | **`npm run obsidian-leak:gate`**＋ `npm run gate`（里也会跑）                                                    |
| Phase1     | P0        | 边界简化（禁深导入硬闸）                                   | ✅ check-boundaries 已跑通       | `obsidian-leak:gate` 内部调用 `scripts/check-boundaries.ts`，本次执行没有深导入违规                                                                             | `gates/` / `scripts/*gate*.mjs` / `scripts/check-boundaries.ts`                            | 🔥  | `npm run check:boundaries` 通过；相关 gate 不被深导入卡住                | **`npm run check:boundaries`** 或直接 `npm run gate`                                                         |
| Phase1     | P0        | 目录整理（功能目录归一 + import 收口）                       | 🟡 结构雏形有，没系统迁移清单             | `features` 目录已建，但还没“模块迁移清单 + 完成定义”；偏工程优化，不挡 thinktxt MVP                                                                                        | 多模块目录                                                                                      | ⚠️  | 文档 + 至少 1~2 模块迁移清单                                           | 无专用测试；**改目录前后跑一轮**：`npm run typecheck && npm run test:unit && npm run gate`                               |
| Phase1     | P1        | IconAction（统一 IconButton+Tooltip）              | ✅ 门禁确认，无旧模式残留                | `npm run iconaction:gate` OK，静态扫旧用法=0                                                                                                           | `IconAction.tsx` + `iconaction-gate`                                                       | 🔥  | 旧模式清零；`iconaction:gate` + `npm run gate` 通过                  | 日常只需 **`npm run iconaction:gate`**（或一起跑 `npm run gate`）                                                   |
| Phase1     | P1        | FilterPopoverShell                             | ❌ 未开始（仓库无文件）                 | 尚无实现，对 thinktxt MVP 不构成阻塞                                                                                                                       | `FilterPopoverShell.tsx`（未来）                                                               | ⚠️  | Shell 可用；替换 ≥2                                               | 实现后建议补 1 条组件测试；当前无测试需求                                                                                    |
| Phase1     | P2        | 竞态/取消统一（takeLatest/Abort）                      | 🟡 单测+部分 gate OK，「专项 gate」未做 | `takeLatest.test.ts` PASS，`events-boundary:gate` ✅；但“全链路取消策略/专项 gate”未落地                                                                        | usecases/hooks                                                                             | 🔥  | 关键链路可取消；unmount 安全；竞态/生命周期/legacy 专项 gate 落地                 | 现阶段：**`npm run test:unit`**（关注 `takeLatest` 用例）＋ 将来为关键链路补 1–2 条集成/E2E                                     |
| **Phase2** | **P0**    | **Legacy block 支持 callout 编辑态（> [!thinktxt]）** | 🔴 单测失败，语义未达标                | `legacyCalloutBlock.test.ts` 失败：`item!.content` 为空，没预期 `- [x]`；说明 `[!thinktxt]` callout 场景下任务抽取语义不对                                             | `src/core/utils/parser.ts`                                                                 | 🔥  | `legacyCalloutBlock` 相关单测全 PASS；DataStore 正确提取新格式            | 重点：**`npm run test:unit`**，直到 `legacyCalloutBlock.test.ts` 绿；必要时补几条 legacy 场景 UT                          |
| Phase2     | P1        | 预览态表格只读渲染（`<!-- start -->...<!-- end -->`）     | ⏳ 设计/未有集成验收                  | 目前没有能直接覆盖这块逻辑的 integration/E2E，用例；`test:unit` 覆盖不到 preview DOM 替换                                                                               | platform post processor + shared 表格组件                                                      | ⚠️  | 预览可见表格（至少 1 条完整用例）                                           | 未来要补：1 条集成测试（跑 postProcessor，看 DOM 里出现 table）＋ 1 条 E2E（打开预览能看到表格）                                         |
| Phase2     | P2        | 表格编辑 + 写回 md（按 start/end 精准替换）                 | ⏳ 未实现                        | 没有表格编辑/写回相关实现和测试；这是快捷编辑核心                                                                                                                       | platform patcher（未来）                                                                       | 💥  | 读→改→写稳定（含测试）                                                 | 规划：UT（金样本 md 读→改→写）、IT（DataStore + patcher）、E2E（编辑→ md 改）——实现时一起补                                         |
| Phase2     | P2        | 拖拽排序 + 写回                                      | ⏳ 未实现                        | 没有拖拽排序实现和测试；依赖表格读写地基                                                                                                                            | dnd + patcher                                                                              | 💥  | 顺序可持久化（含测试）                                                  | 同上，和表格编辑共用 patcher 的 UT/IT/E2E，只是多一些 reorder 场景                                                           |
| Phase3     | P1        | DataStore 拆分（Repo/Cache/Indexer）               | 0/4（MVP 可后置）                 | 现在 DataStore 仍是“单块”，未见拆分提交；后期优化                                                                                                                 | `src/core/services/DataStore.ts`                                                           | ⚠️  | —（靠现有用例保护）                                                   | 不增加新测试类型；**重构时必须跑全套**：`typecheck + test:unit + gate + 关键 E2E`                                             |
| Phase4     | P1        | 工程收尾文档化                                        | 🟡 docs 存在，闭环未验证             | `npm run build` 成功；但没验证“新人按 README 从 install→check/test 的闭环”                                                                                    | `docs/` / `README`                                                                         | 🙂  | 新人按文档可跑 build/check/gate/test                                | 手动按 README 跑一轮：`npm install && npm run build && npm run check && npm run test:unit && npm run gate`，过程记一下 |
| （旧表补回）     | P2        | shared 去平台化（obsidian-leak gate）                | ✅ 本次已实测 PASS                 | `npm run obsidian-leak:gate` ✅：平台外 obsidian import 仅在 allowlist 内，无新增泄漏                                                                         | `shared/ui/markdown` / `scripts/obsidian-leak-gate.mjs`                                    | 🔥  | `npm run obsidian-leak:gate` PASS                            | 和“共享层去平台化”一项视为同一块：稳定后只需定期跑 `npm run obsidian-leak:gate`                                                   |
| （旧表补回）     | Phase1 P0 | gate 全绿（总门禁）                                   | 🔴 被 `di-resolve:gate` 卡住    | `npm run gate` 在 `di-resolve:gate` 阶段失败：`src/main.ts` 存不允许的 `container.resolve()`；后续 gate（modal-promise / selector-* / theme-* / iconaction）没跑到 | `scripts/*gate*.mjs` / `src/main.ts`                                                       | 🔥  | `npm run gate` 全绿；composition root 之外无非法 `container.resolve` | **`npm run gate`**；修掉 `di-resolve:gate` 提示的问题，再确认其他 gate 全部跑完且通过                                          |
