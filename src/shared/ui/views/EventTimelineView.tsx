// Thin forwarder to keep legacy imports stable.
// Note: do NOT import from './EventTimelineView' here (it would resolve to this file).
export { EventTimelineView } from './EventTimelineView/index';
