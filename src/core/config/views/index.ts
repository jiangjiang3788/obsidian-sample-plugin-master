/**
 * Core view-configuration facade.
 *
 * Defaults, export presets and view-config types are split by ownership here;
 * src/core/config/viewConfigs.ts remains as the compatibility entrypoint.
 */
export * from './types';
export * from './defaults';
export * from './exportConfigs';
