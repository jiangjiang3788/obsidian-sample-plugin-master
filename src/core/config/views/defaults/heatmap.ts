import type { HeatmapViewConfig } from '../types';

export const HEATMAP_VIEW_DEFAULT_CONFIG: HeatmapViewConfig = {
  displayMode: 'habit',
  sourceBlockId: '',
  themePaths: [],
  maxDailyChecks: 10,
  allowManualEdit: true,
};
