# 统一输入改造 · 第 1 轮现状盘点报告（只做“看清”）

> 目标：完成 5 张表的第一版——**写入口清单、UseCase 清单、校验/补全清单、副作用清单、竞态热点清单**。  
> 范围：基于上传代码包中与下列目录直接相关的实现进行静态盘点：
>
> - `app/ui/components/QuickInputEditor/*`
> - `features/quickinput/*`
> - `features/settings/*`
> - `app/usecases/*`
> - `app/store/slices/*`
> - `core/services/InputService.ts`
> - `core/utils/inputTemplateUtils.ts`
> - `core/utils/normalize.ts`
> - `core/types/quickInput.ts`
> - 同时补看了与写链直接相连的：`core/services/ActionService.ts`、`core/services/ItemService.ts`、`core/services/SettingsRepository.ts`、`core/services/DataStore.ts`、`platform/modals/*`、`platform/events/VaultWatcher.ts`、`app/bootstrap/initializeCore.ts`

---

## 0. 我认为什么叫“详尽”

这类报告里，“详尽”不是把文件名堆满，而是至少满足这 5 条：

1. **覆盖够广**：你指定的关键目录都看过，而且不是只看文件名，而是能说明“谁在写、写到哪、怎么写”。
2. **链路够完整**：每条关键写入路径都要落到“UI 触发 → UseCase/Service → Store/Repository/Vault → 刷新/通知”。
3. **证据够硬**：重要结论必须能回指到文件和代码位置，而不是凭感觉。
4. **风险够具体**：不仅说“有副作用/有竞态”，还要指出**哪条链**、**为什么会发生**、**最低处理标准是什么**。
5. **边界够清楚**：哪些地方已确认，哪些地方只是高概率推断，要分开写。

按这个标准，这份报告给的是：

- **覆盖面**：已覆盖你指定目录的主干写链，并额外补了直接相连的核心服务/平台事件链。
- **证据形式**：以下所有关键结论都带 `文件:行号`。
- **结论粒度**：不仅有“清单”，还有“归类”“绕过点”“副作用归属现状”“竞态热点”。

---

## 1. 先给结论摘要

### 1.1 当前系统里，实际上已经存在两条主写链

**链 A：设置/配置写链**  
`UI(features/settings 等) → useCases.* → zustand slice / state.updateSettings → SettingsRepository.update() → persistence → repository.subscribe → zustand.setState → UI/运行时订阅副作用`

证据：
- 架构文档要求 features 通过 useCases 写，而不是直接触 slices：`app/ARCH_CONSTRAINTS.md:40-59`
- `useCases` 明确宣称是 UI 唯一业务入口：`app/usecases/index.ts:6-22, 76-85`
- `SettingsRepository.update()` 是统一设置持久化入口：`core/services/SettingsRepository.ts:138-159`
- `initializeCore()` 订阅 `SettingsRepository`，把 settings 同步回 zustand，并挂接 timer widget / devConsole / categoryColors 等运行时副作用：`app/bootstrap/initializeCore.ts:74-130`

**链 B：记录/输入写链**  
`UI/Modal → InputService / ItemService → Vault 写文件 → DataStore.scanFileByPath()/notifyChange() → 视图刷新`

证据：
- `QuickInputModal` 提交时直接调 `inputService.updateExistingRecord()` / `inputService.executeTemplate()`，并自行 `scanFileByPath + notifyChange`：`platform/modals/QuickInputModal.tsx:492-527`
- `InputService` 自身直接写 vault；编辑路径里还内置 `scanFileByPath + notifyChange`：`core/services/InputService.ts:16-40, 42-79`
- `ItemService.completeItem()/updateItemTime()` 也是直接改文件，再 `scanFileByPath().then(notifyChange)`：`core/services/ItemService.ts:33-46, 51-99, 121-149`

### 1.2 你说的风险已经在代码里落地了：**“统一写入口”在设置域成立，在输入域不成立**

- 设置域多数动作确实走 `useCases.*`。
- 但记录输入域并**没有**一个 usecase 层的统一动作内核；真实写入核心在 `InputService` / `ItemService`，而且入口分散在多个 Modal / View / Timer 流程中。

### 1.3 文档与现实有两处明显漂移

**漂移 1：QuickInputModal 注释与实现不一致**  
文件头注释写的是“使用 useCases 进行写入操作”，但真实代码直接调用的是 `InputService`，没有经过任何输入 usecase：
- 注释：`platform/modals/QuickInputModal.tsx:3-6`
- 实现：`platform/modals/QuickInputModal.tsx:492-527`

**漂移 2：Layout/View 规范与实现不一致**  
架构文档与 `layout.usecase.ts` 注释都说 features 层应只走 `useCases.layout.*`，甚至把 `viewinstance.usecase` 标成“已禁用/不允许 features 直调”：
- 规范：`app/ARCH_CONSTRAINTS.md:142-172, 187-195`
- `layout.usecase.ts` 注释：`app/usecases/layout.usecase.ts:6-18`

但现实里：
- `LayoutSettings` 直接调用 `useCases.viewInstance.createView/updateView`：`features/settings/LayoutSettings.tsx:105-117, 157-166`
- `LayoutEditorPanel` 也直接调用 `_useCases.viewInstance.createView/updateView`：`features/settings/components/LayoutEditorPanel.tsx:138, 186`
- `ModuleSettingsModal` 每次改动都直接 `useCases.viewInstance.updateView`：`features/settings/ModuleSettingsModal.tsx:45-48`
- `LayoutRenderer` 删除 view 时直接 `useCases.viewInstance.deleteView`：`features/settings/LayoutRenderer.tsx:403-406`

更关键的是：**`layout.usecase.ts` 里并没有文档声称的 addView/updateView/deleteView 等 facade 方法**；当前 features 只能绕去 `viewinstance.usecase.ts`。这说明 ViewInstance 侧的“唯一写入口”并没有真正收口。

---

## 2. 范围与方法

本轮是**静态盘点**，不做改码，不做行为回归，不做运行时打点。方法是：

1. 先读架构约束和 usecase 出口，确定“理论上的唯一写入口”是什么。
2. 再从 UI 层反查所有写入口：
   - `new QuickInputModal(...)`
   - `useCases.*.*(...)`
   - `inputService.executeTemplate/updateExistingRecord(...)`
   - `itemService.completeItem/updateItemTime(...)`
3. 再把这些入口落到实际持久化位置：
   - `SettingsRepository.update()`
   - `vault.writeFile()`
4. 最后补副作用与竞态：
   - `scanFileByPath`
   - `notifyChange`
   - `createTakeLatest`
   - repository/store subscriptions

---

## 3. 写入口清单（第一版）

下面的“写入口”分成两大类：

- **A. 记录/输入域**：真正影响 md 记录内容
- **B. 设置/配置域**：影响模板、主题、布局、视图、AI 设置、计时器设置等

---

## 3A. 记录 / 输入域写入口

### A-1. QuickInput 命令入口

| 入口 | 触发文件 | 写动作最终去向 | 备注 |
|---|---|---|---|
| 命令面板“快速录入 - {block}” | `features/quickinput/registerCommands.ts:27-35` | `QuickInputModal` → `InputService.executeTemplate/updateExistingRecord` | 最基础入口 |
| 平台抽象 `openQuickInput()` | `platform/ObsidianModalPort.ts:58-60` | 同上 | 平台侧统一打开 modal，但不统一写动作 |

### A-2. 视图/列表中打开 QuickInput 的入口

| 入口 | 触发文件 | 目的 | 最终写链 |
|---|---|---|---|
| 任务行点击编辑 | `shared/ui/items/TaskRow.tsx:34-40` | 编辑已有任务 | `QuickInputModal(mode='edit')` → `InputService.updateExistingRecord()` |
| Block 行点击编辑 | `shared/ui/items/BlockItem.tsx:26-33` | 编辑已有 block 记录 | 同上 |
| Heatmap 空白格新增 / 单条格继续录入 | `shared/ui/views/HeatmapView.tsx:97-140` | 新建记录 | `QuickInputModal(create)` → `InputService.executeTemplate()` |
| Timeline 点击空白时间段建任务 | `shared/ui/views/timelineInteraction.ts:26-74` | 新建任务 | 同上 |
| LayoutRenderer 的快捷输入按钮 | `features/settings/LayoutRenderer.tsx:185-190, 369-373` | 新建记录 | 同上 |
| Timer 浮窗“新任务” | `features/timer/TimerViewView.tsx:23-31` | 先返回 `QuickInputSaveData`，再由 `TimerService` 新建并开计时 | `QuickInputModal(onSave)` → `TimerService.createNewTaskAndStart()` → `InputService.executeTemplate()` |
| Timer 行“编辑任务” | `features/timer/TimerRow.tsx:56-63` | **看起来像编辑，但实际不是 edit mode** | `QuickInputModal(create)`，只传了 `blockId + context`，没传 `{ mode:'edit', editItem }` |

#### 对 A-2 的判断

`TaskRow` / `BlockItem` 是**标准编辑链**：
- 直接以 `mode:'edit'` 打开：`shared/ui/items/TaskRow.tsx:37-40`、`shared/ui/items/BlockItem.tsx:30-33`

`TimerRow` 是一个**明显不一致点**：
- 它调用 `actionService.getQuickInputConfigForTaskEdit(taskId)` 拿到的是 `blockId + context`：`core/services/ActionService.ts:134-168`
- 但真正打开 modal 时并没有传 `mode:'edit'` 和 `editItem`：`features/timer/TimerRow.tsx:56-63`
- 这意味着它更像“带上下文的新建壳”而不是“回写原记录”。

这个点需要你在第 2 轮定规则时单独裁决：**TimerRow 的“编辑任务”究竟应不应该统一到真正的 update 链。**

### A-3. QuickInputModal 本身就是一个写根，而不是纯壳

`QuickInputModal` 当前不是一个“只收集输入、交给统一内核处理”的前台壳，而是**自己就包含了完整提交逻辑**：

- 读 editorState：`platform/modals/QuickInputModal.tsx:492-501`
- 走 `onSave` 分支（Timer 新任务）：`platform/modals/QuickInputModal.tsx:503-506`
- 走 edit 分支：`platform/modals/QuickInputModal.tsx:511-516`
- 走 create 分支：`platform/modals/QuickInputModal.tsx:517-523`
- toast + close：`platform/modals/QuickInputModal.tsx:526-533`

所以从改造角度看，**QuickInputModal 现在并不是“入口壳”，而是“入口 + 提交控制器 + 副作用协调器”混在一起**。

### A-4. AI 批量确认也是独立输入写链

`AiBatchConfirmModal` 不是走 `QuickInputModal`，而是自己直接：

- 解析模板：`platform/modals/AiBatchConfirmModal.tsx:218-225, 256-260`
- `inputService.executeTemplate()`：`platform/modals/AiBatchConfirmModal.tsx:227-231, 262-264`
- 成功后仅 `notifyChange()`：`platform/modals/AiBatchConfirmModal.tsx:231, 271`

这里有两个重要现象：

1. **它和 QuickInput 共用了模板解析 / form finalize，但没共用提交流程。**
2. **它 create 后没有主动 `scanFileByPath()`，只 `notifyChange()`。**  
   相比之下，QuickInput create 路径会显式 `scanFileByPath + notifyChange`：`platform/modals/QuickInputModal.tsx:518-523`

这说明当前“创建记录后的刷新策略”并不统一。

### A-5. TimerService 也在直接落库

`TimerService.createNewTaskAndStart()`：
- 直接 `inputService.executeTemplate(...)`：`features/timer/TimerService.ts:144-148`
- 手动 `dataStore.scanFileByPath(targetFilePath)`：`features/timer/TimerService.ts:149-153`
- 再决定是 `startOrResume(latestItem.id)` 还是 `notifyChange()`：`features/timer/TimerService.ts:154-160`

这条链本质上也是一个**独立创建入口**。

### A-6. 任务完成/时间修改是另一条记录写链，不走 QuickInput

| 动作 | 入口 | 最终服务 |
|---|---|---|
| 勾选完成任务 | `features/settings/LayoutRenderer.tsx:376-378` | `itemService.completeItem()` |
| 视图中更新时间 | `features/settings/LayoutRenderer.tsx:178-180` | `itemService.updateItemTime()` |
| 计时结束写回时长 | `features/timer/TimerService.ts:124-131` | `itemService.completeItem()` 或 `itemService.updateItemTime()` |

而 `ItemService` 内部是：
- 直接读写目标文件行：`core/services/ItemService.ts:20-45`
- 完成任务：`core/services/ItemService.ts:51-101`
- 更新时间：`core/services/ItemService.ts:121-136`
- 写完后 `scanFileByPath().then(notifyChange())`：`core/services/ItemService.ts:45-46, 147-148`

### A-7. “删除记录”写入口：本轮未找到统一主线

本轮代码盘点里，**没有找到与 `InputService.updateExistingRecord()` 对应的 “deleteExistingRecord” 或统一的记录删除 usecase**。

已确认：
- 配置删除（block/theme/layout/view）很多；
- 记录编辑/创建很多；
- 但“删除一条已存在记录”的统一主线，在本轮范围内没有看见。

这不是说系统一定不能删记录，而是说：**在当前盘点范围里，没有看到一个成型、显性的、可复用的记录删除主动作。**

这对你第 2 轮定动作模型是重要输入：`submitDeleteRecord` 目前很可能需要新建，而不是简单抽取。

---

## 3B. 设置 / 配置域写入口

这一组总体比输入域更规整，大部分已经经过 `useCases.*`。

### B-1. Block 相关

`features/settings/BlockManager.tsx`

- add：`useCases.blocks.addBlock()`：`features/settings/BlockManager.tsx:121-127`
- delete：`useCases.blocks.deleteBlock()`：`features/settings/BlockManager.tsx:129-134`
- duplicate：`useCases.blocks.duplicateBlock()`：`features/settings/BlockManager.tsx:136-139`
- reorder：`useCases.blocks.reorderBlocks()`：`features/settings/BlockManager.tsx:147-152`
- inline update：`useCases.blocks.updateBlock()`：`features/settings/BlockManager.tsx:73-77`
- 触发方式主要是 `onBlur` 持久化：`features/settings/BlockManager.tsx:75-100`

### B-2. Theme / Override 相关

- 新增主题：`features/settings/ThemeMatrixView.tsx:131, 181-184`
- 行内改 icon/path：`features/settings/ThemeTreeNodeRow.tsx:162, 178`
- 保存 override：`features/settings/TemplateEditorModal.tsx:53-71, 143`
- 批量主题操作：`features/settings/useBatchOperations.ts:54-84`

### B-3. Settings 相关

- 通用设置页：
  - 分类颜色：`features/settings/GeneralSettings.tsx:43, 50, 58`
  - 悬浮计时器开关：`features/settings/GeneralSettings.tsx:70`
  - dev console stack 开关：`features/settings/GeneralSettings.tsx:83`
- AI 设置保存：`features/settings/AiSettings.tsx:73-74, 457`
- 统计视图容器还会**自动补分类颜色并持久化**：`shared/ui/views/StatisticsView/StatisticsViewContainer.tsx:80-94`

这说明“设置写入口”并不只在 settings 页，**shared/ui 也有自动写入全局设置的行为**。

### B-4. Layout / ViewInstance 相关

#### Layout 相关（相对规范）

- `LayoutSettings`：
  - update layout：`features/settings/LayoutSettings.tsx:59-61`
  - delete layout：`features/settings/LayoutSettings.tsx:266-270`
  - duplicate layout：`features/settings/LayoutSettings.tsx:272-274`
  - rename layout：`features/settings/LayoutSettings.tsx:276-280`
  - add layout：`features/settings/LayoutSettings.tsx:338-351`
  - reorder layouts：`features/settings/LayoutSettings.tsx:366-378`
- `LayoutRenderer`：
  - selectedThemes：`features/settings/LayoutRenderer.tsx:408-412`
  - selectedCategories：`features/settings/LayoutRenderer.tsx:414-418`
- `LayoutEditorPanel`：
  - update layout：`features/settings/components/LayoutEditorPanel.tsx:70`

#### ViewInstance 相关（存在明显绕行）

- create view：`features/settings/LayoutSettings.tsx:105-117`、`features/settings/components/LayoutEditorPanel.tsx:138`
- update view title：`features/settings/LayoutSettings.tsx:157-166`、`features/settings/components/LayoutEditorPanel.tsx:186`
- delete view：`features/settings/LayoutRenderer.tsx:403-406`
- `ModuleSettingsModal` 每次改动即持久化：`features/settings/ModuleSettingsModal.tsx:45-48, 84-187`

这里的核心问题不是“有没有 usecase”，而是：

- 文档希望 **Layout facade 一统 Layout + View 写入口**；
- 现实是 **features 直接调用 `useCases.viewInstance.*`**；
- 而 `viewinstance.usecase.ts` 本身又直接用的是**通用 escape hatch** `state.updateSettings(...)`：`app/usecases/viewinstance.usecase.ts:38-41, 58-62, 77-89`

所以 ViewInstance 这块当前是**名义有 usecase，实际上没真正收成一个稳定 facade**。

---

## 4. UseCase 清单（系统已有“标准动作”）

## 4.1 已存在的 usecase 及职责

### `BlocksUseCase`
- 方法：`reorderBlocks / addBlock / updateBlock / deleteBlock / duplicateBlock / moveBlock`
- 证据：`app/usecases/blocks.usecase.ts:25-171`
- 覆盖：Block 管理基本完整

### `ThemeUseCase`
- 方法：`addTheme / updateTheme / deleteTheme / batchUpdateThemes / batchDeleteThemes / batchUpdateThemeStatus / batchUpdateThemeIcon / upsertOverride / deleteOverride / batchUpsertOverrides / batchDeleteOverrides / batchSetOverrideStatus`
- 证据：`app/usecases/theme.usecase.ts`（方法清单可由 `rg` 得到，已在盘点中确认）
- 覆盖：Theme / Override 基本完整

### `LayoutUseCase`
- 方法：`addLayout / updateLayout / deleteLayout / moveLayout / duplicateLayout / reorderLayouts / batchUpdateLayouts / batchDeleteLayouts / moveLayoutToParent / addViewInstanceToLayout / removeViewInstanceFromLayout / reorderViewInstancesInLayout / get*`
- 证据：`app/usecases/layout.usecase.ts:43-260+`
- 覆盖：Layout 很完整；View 与 Layout 关系只覆盖“挂接/移除/重排”，**没覆盖 ViewInstance CRUD**

### `ViewInstanceUseCase`
- 方法：`createView / updateView / deleteView`
- 证据：`app/usecases/viewinstance.usecase.ts:17-95`
- 特点：依赖 `state.updateSettings()` 通用 mutator，而不是专门 slice action：`app/usecases/viewinstance.usecase.ts:38-41, 58-62, 77-89`

### `SettingsUseCase`
- 方法：`setFloatingTimerEnabled / setDevConsoleStackEnabled / toggleTimerWidgetVisibility / updateAiSettings / updateCategoryColors`
- 证据：`app/usecases/settings.usecase.ts` 方法清单已确认

### `TimerUseCase`
- 方法：`getTimers / setInitialTimersFromDisk / addTimer / updateTimer / removeTimer`
- 证据：`app/usecases/timer.usecase.ts:12-54`

## 4.2 关于“统一输入”最关键的结论

**当前不存在一个对等于 `BlocksUseCase` / `ThemeUseCase` / `LayoutUseCase` 的“InputUseCase”或“RecordInputUseCase”。**

也就是说：

- 配置域已经有 facade；
- 输入域没有对应 facade；
- QuickInput / AI Batch / Timer New Task / Item time update 都各自直接连到 service 层。

这就是为什么你现在强调“不要做第二个前台输入渠道，而是先建统一输入内核”是对的：

> 因为现在缺的不是“再多一个按钮”，缺的是**输入域自己的应用层 facade / 动作模型**。

---

## 5. 校验 / 补全 / 归一化清单

这一块是本轮最重要的“分散逻辑盘点”。结论先说：

> **模板解析、表单默认值、编辑态回填、字段对象化、时间联动、主题 override 解析、视图配置 normalize，已经散落在多个层级。**

不是一个点坏了，而是**同一类规则分布在 6 个地方**。

## 5.1 模板解析：`TemplateResolver` 是当前最接近“统一真源”的点

- 唯一模板解析实现：`core/services/TemplateResolver.ts:17-31`
- 解析规则：
  - 找 base block：`core/services/TemplateResolver.ts:46-50`
  - 找 theme：`core/services/TemplateResolver.ts:52-55`
  - 有 override 且未禁用则合并 `fields/outputTemplate/targetFile/appendUnderHeader`：`core/services/TemplateResolver.ts:57-74`
  - 否则返回 baseBlock：`core/services/TemplateResolver.ts:77-78`
- `inputTemplateUtils.ts` 现在只是 thin wrapper：`core/utils/inputTemplateUtils.ts:4-5, 24-30`

### 这部分当前状态

这已经比较收口。后续统一输入内核应该**直接复用它**，而不是再发明一套 block/theme/override 合并规则。

## 5.2 QuickInputEditor：默认值、context 回填、时间联动都在这里

`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx`

### 已确认的逻辑

- 提交前 finalize：删 `lastChanged` + 统一时间字段：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:15-20`
- 过滤当前 block 下被 disabled 的主题：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:76-88`
- 取 effective template：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:90-94`
- context 回填：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:118-139`
- defaultValue 回填：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:140-152`
- 无 defaultValue 时自动填当前日期/当前时间/首个选项：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:153-160`
- 时间联动（时间/结束/时长）：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:168-174`
- block 切换时只保留特定字段：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:192-200`
- `templateId/templateSourceType` 会随 editor state 一起对外发：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx:176-186`

### 判断

QuickInputEditor 现在已经承担了很大一部分“输入规则层”职责，但它仍然是 UI 组件，不是独立输入内核。

## 5.3 QuickInputModal：编辑态模板推断和回填也在这里

`platform/modals/QuickInputModal.tsx`

### 已确认逻辑

- `mapFieldValue()`：把 select/radio/rating/path-like 字段映射成 `{value,label}`：`platform/modals/QuickInputModal.tsx:214-236`
- `scoreTemplateForItem()`：根据 `templateId/categoryKey/outputTemplate/field token` 给 block 打分：`platform/modals/QuickInputModal.tsx:269-300`
- `resolveBlockForEdit()`：exact → inferred → fallback：`platform/modals/QuickInputModal.tsx:303-326`
- `buildInitialFormData()`：从 item.extra / item 本体读取字段并映射：`platform/modals/QuickInputModal.tsx:341-389`
- `resolveEditState()`：决定 block/theme/override/title/initialFormData：`platform/modals/QuickInputModal.tsx:391-428`

### 判断

这说明“编辑已有记录时，应该用哪个模板、如何从旧 item 反推表单值”这套规则**没有独立抽出来**，还嵌在 modal 里。

这部分将来非常适合作为 `resolveEditRecordContext` / `buildRecordFormStateFromItem` 之类的统一动作拆出来。

## 5.4 InputService：提交时再次做字段对象归一化

`core/services/InputService.ts`

- `buildRenderData()` 会对 option object 做再次归一化：`core/services/InputService.ts:81-115`
- `ratingPair` 会额外写入辅助键（默认 `评图`）：`core/services/InputService.ts:97-103`
- path/select/radio 也会保留 `{label,value}`：`core/services/InputService.ts:103-105`
- 最终统一补入 `block/theme/templateId/templateSourceType`：`core/services/InputService.ts:108-114`

### 判断

这部分很重要，因为它说明：

- UI 层已经在处理 option object；
- service 层又做了一遍“面向渲染模板”的对象归一化；
- 这其实已经接近“normalizeRecordInput”的后半段。

## 5.5 Theme / Block / Layout 的校验与默认值分散在 slices

### Theme
- `addTheme`：空路径拒绝、重复路径拒绝：`app/store/slices/theme.slice.ts:90-107`
- `updateTheme`：改 path 时检查重复：`app/store/slices/theme.slice.ts:136-149`
- 删 theme 时级联删 overrides：`app/store/slices/theme.slice.ts:170-186`
- 批量删 theme 时也删 overrides：`app/store/slices/theme.slice.ts:221-233`

### Block
- `addBlock` 默认新对象：`categoryKey=name`，其余 `targetFile/appendUnderHeader/outputTemplate/fields` 默认空：`app/store/slices/blocks.slice.ts:63-71`
- 删 block 时级联删 overrides：`app/store/slices/blocks.slice.ts:125-148`

### Layout
- `addLayout` 默认 `viewInstanceIds=[] / displayMode='list' / initialView='月' / initialDateFollowsNow=true`：`app/store/slices/layout.slice.ts:98-125`
- `deleteLayout` 只删 layout 本身，不处理 viewInstances：`app/store/slices/layout.slice.ts:161-175`

## 5.6 视图编辑器里的 normalize 也很分散

### HeatmapViewEditor
- `normalizeHeatmapConfig()`：`features/settings/HeatmapViewEditor.tsx:21-34`
- `handleScanThemes()`：从数据源扫描并写回 `themePaths`：`features/settings/HeatmapViewEditor.tsx:46-75`

### StatisticsViewEditor
- 自动发现分类并写回 `categories`：`features/settings/StatisticsViewEditor.tsx:19-43`
- 调整顺序/别名即时 `onChange`：`features/settings/StatisticsViewEditor.tsx:45-62`

### TimelineViewEditor
- `normalizeProgressOrder()`：`features/settings/TimelineViewEditor.tsx:23-84`
- 分类改名碰撞保护：`features/settings/TimelineViewEditor.tsx:56-61`
- 所有 patch 只发最小补丁：`features/settings/TimelineViewEditor.tsx:303-306`

### 判断

这些逻辑虽然不属于 quickinput 表单本身，但它们说明：

- **“配置输入的 normalize”** 也没有统一层；
- 现在很多 editor 组件自己带规则；
- 将来如果你想把 “view/editor/modal 里的编辑动作” 一起纳入统一动作模型，就不能只盯 quickinput。

## 5.7 `core/utils/normalize.ts` 不是输入 normalize，而是 item 日期 normalize

`core/utils/normalize.ts` 当前只做：
- block/task 的统一日期字段推导：`core/utils/normalize.ts:9-46`

它**不是**输入表单 normalize，也不能直接拿来当“统一输入 normalize 层”。

---

## 6. 副作用清单（写后会发生什么）

这一节是你这次特别要求纳入的重点。

## 6.1 设置写链的副作用

### 固定链路
1. `useCases.*` 调 store action / `updateSettings`
2. 底层 `SettingsRepository.update()` 保存：`core/services/SettingsRepository.ts:138-159`
3. repository `notify()`：`core/services/SettingsRepository.ts:63-70, 149-155`
4. `initializeCore()` 里订阅 repository，把 settings 同步回 zustand：`app/bootstrap/initializeCore.ts:74-79`

### 附加运行时副作用
- `floatingTimerEnabled` 变化：创建/卸载 timer widget，并同步 UI 可见性：`app/bootstrap/initializeCore.ts:81-111`
- `devConsoleStackEnabled` 变化：切换 dev console 行为：`app/bootstrap/initializeCore.ts:112-120`
- `categoryColors` 变化：同步运行时颜色映射：`app/bootstrap/initializeCore.ts:122-130`

### 结论

设置写链的副作用**已经有“固定承接点”**，也就是 bootstrap 层的订阅。这是当前架构中比较健康的一部分。

## 6.2 QuickInput create 的副作用

`platform/modals/QuickInputModal.tsx:517-527`

create 成功后会：
1. `inputService.executeTemplate(...)`
2. `dataStore.scanFileByPath(path)`
3. `dataStore.notifyChange()`
4. `new Notice('✅ 已保存')`
5. `closeModal()`

## 6.3 QuickInput edit 的副作用

`platform/modals/QuickInputModal.tsx:511-527`

edit 成功后会：
1. `inputService.updateExistingRecord(...)`
2. `InputService.updateExistingRecord()` 内部又 `scanFileByPath + notifyChange`：`core/services/InputService.ts:74-78`
3. `new Notice('✅ 已写回 md 并刷新记录')`
4. `closeModal()`

## 6.4 AI 批量确认的副作用

`platform/modals/AiBatchConfirmModal.tsx:211-272`

- 单条保存：`executeTemplate` → 更新当前记录状态 → Notice → `notifyChange` → 跳下一条
- 批量保存：逐条 `executeTemplate` → 末尾统一 Notice → `notifyChange`

### 明显差异

AI 批量保存没有显式 `scanFileByPath()`，而 QuickInput create / Timer 新建有。

这意味着“写入后刷新”的副作用策略至少有三种：

1. 写完立即 scan + notify
2. 写完只 notify
3. 写完 scan，若识别到 item 则继续触发 timer 业务

## 6.5 TimerService 的副作用

`features/timer/TimerService.ts`

- `startOrResume()`：如果已有 running timer，会先 pause 其它 timer：`features/timer/TimerService.ts:38-65`
- `stopAndApply()`：不仅改 md，还会 `ui.notice()` 并删除 timer：`features/timer/TimerService.ts:97-137`
- `createNewTaskAndStart()`：创建 md → scan file → 尝试自动 start timer，否则 notice + notify：`features/timer/TimerService.ts:144-165`

## 6.6 ItemService 的副作用

`core/services/ItemService.ts`

- 完成任务 / 更新时间 / 插入完成记录，最终都会：
  - `vault.writeFile()`
  - `dataStore.scanFileByPath(path).then(() => dataStore.notifyChange())`

证据：`core/services/ItemService.ts:33-46, 141-149`

## 6.7 VaultWatcher 带来的“平台同步副作用”

Dashboard 初始化时创建了 `VaultWatcher`：`features/settings/index.ts:102-113`

`VaultWatcher` 对 vault create/modify/delete/rename 都会推动 `DataStore`：
- create/modify：`platform/events/VaultWatcher.ts:27-30`
- delete：`platform/events/VaultWatcher.ts:34-38`
- rename：`platform/events/VaultWatcher.ts:42-46`

### 重要判断（带推断性质）

从代码结构看，**应用自身写文件后，很可能还会触发 VaultWatcher 的扫描/通知**。  
如果 Obsidian 对自身写入也触发 markdown create/modify 事件，那么：

- QuickInput create：手动 scan/notify + watcher scan/notify
- QuickInput edit：service 内 scan/notify + watcher scan/notify
- ItemService.complete/update：service 内 scan/notify + watcher scan/notify

这意味着当前存在**重复扫描 / 重复通知**的高概率风险。  
这条结论是基于代码结构的合理推断，不是运行时实测结论；但在第 2 轮定副作用归属时必须考虑。

---

## 7. 竞态 / 防重热点清单

## 7.1 QuickInputModal：有一半防重，但不完整

### 已做的
- 用 `createTakeLatest('quick-input-submit')` 保证 submit 取最后一次：`platform/modals/QuickInputModal.tsx:457, 509-517`
- `createTakeLatest` 会 cancel 上一个任务，并把 `AbortError` 转成 `CancelledError`：`shared/utils/takeLatest.ts:20-65`
- `isSubmitting` 会禁用按钮：`platform/modals/QuickInputModal.tsx:473, 564-569`
- `InputService.updateExistingRecord()` 支持 `AbortSignal`：`core/services/InputService.ts:42-79, 133-139`

### 还没做完整的地方
- create 分支 `executeTemplate()` **不接收 signal**：`platform/modals/QuickInputModal.tsx:518-523`、`core/services/InputService.ts:16-40`
- 也就是说：**edit 可中断，create 不可中断**

### 判断

QuickInput 现在的“防重”更准确地说是：
- UI 按钮层做了锁；
- edit 链做了 takeLatest + abort；
- create 链只做了 UI 锁，没有真正可中断的 service 级提交取消。

## 7.2 AI 批量确认：没有 takeLatest / 没有事务边界 / 允许部分成功

`AiBatchConfirmModal`：`platform/modals/AiBatchConfirmModal.tsx:211-272`

问题：
- 没有 `createTakeLatest`
- `handleSaveAll()` 是顺序循环，一条失败不会回滚前面成功的条目
- `saveAll` 过程没有总级别的提交锁、没有 abort、没有统一刷新策略

这类链路后续应至少纳入“串行提交 + 可中断 + 部分失败结果协议”。

## 7.3 View 设置编辑：几乎是“每次改动立即落库”

`ModuleSettingsModal` 里的 `ViewInstanceEditor`：
- 所有 `onChange` 直接 `useCases.viewInstance.updateView(...)`：`features/settings/ModuleSettingsModal.tsx:45-48, 84-187`
- save 按钮并不真正提交，只是 `await new Promise(resolve => setTimeout(resolve, 100)); onClose();`：`features/settings/ModuleSettingsModal.tsx:208-217`

### 判断

这是一个很明显的竞态/副作用热点：
- 没有“编辑态”和“已提交态”的边界；
- save 只是“等一下然后关窗”；
- 如果你后面想把 modal save 纳入统一动作，这里必须重构。

## 7.4 ViewInstanceUseCase：走通用 `updateSettings`，没有专门并发约束

`app/usecases/viewinstance.usecase.ts:38-41, 58-62, 77-89`

因为 View CRUD 直接用的是 `state.updateSettings(draft => ...)`，而 `SettingsRepository.update()` 当前没有显式队列/锁：`core/services/SettingsRepository.ts:138-159`

这意味着：
- 并发写时只能依赖 JS 事件循环与当前内存快照；
- 没有 recordId/layoutId/viewId 级别串行器；
- 也没有“版本号/冲突检测”。

## 7.5 ItemService：scan/notify 是异步 fire-and-forget

`core/services/ItemService.ts:45-46, 147-148`

这里是：
```ts
this.dataStore.scanFileByPath(path).then(() => this.dataStore.notifyChange());
```

没有 `await`，意味着：
- 调用方拿到 `completeItem/updateItemTime` resolved，并不等于 DataStore 已经刷新完成；
- UI 侧若紧接着读取，可能读到旧缓存；
- 如果同时还有 VaultWatcher 事件，这个顺序会更难推断。

## 7.6 Timer 新建任务链：创建、扫描、找最新 item、启动 timer 是一条复合事务，但没有统一事务边界

`features/timer/TimerService.ts:144-165`

步骤是：
1. create md
2. scan file
3. 从扫描结果里挑 line 最大的 item 当 latest
4. startOrResume(latestItem.id)

热点：
- 如果模板输出多条可解析记录，`line 最大` 未必就是“应该计时的那条”
- 如果 scan 结果为空，会退化成“创建了文件但没识别成任务”
- 没有显式事务对象传递“本次创建的 recordId”

## 7.7 StatisticsViewContainer：会自动写全局颜色设置

`shared/ui/views/StatisticsView/StatisticsViewContainer.tsx:80-94`

它在渲染期 `useEffect` 里自动发现分类并写 `categoryColors`。  
虽然逻辑合理，但从“竞态与副作用归属”角度看，这意味着：

- 纯展示容器也会触发全局设置写入；
- 数据变化可能反向触发设置持久化；
- 这类“渲染即写入”的行为后续要谨慎纳入统一规则。

---

## 8. 绕过点 / 不一致点（最值得你下一轮盯住）

## 8.1 绕过点 1：输入域没有 usecase facade，UI 直接调 service

代表文件：
- `platform/modals/QuickInputModal.tsx:492-527`
- `platform/modals/AiBatchConfirmModal.tsx:211-272`
- `features/timer/TimerService.ts:144-165`
- `core/services/ItemService.ts:51-149`（被多个 UI/feature 间接调用）

## 8.2 绕过点 2：ViewInstance 没有真正并入 Layout facade

代表文件：
- 规范：`app/ARCH_CONSTRAINTS.md:142-172, 187-195`
- 实现：`features/settings/LayoutSettings.tsx:109, 162`、`features/settings/components/LayoutEditorPanel.tsx:138, 186`、`features/settings/ModuleSettingsModal.tsx:47`、`features/settings/LayoutRenderer.tsx:405`
- `viewinstance.usecase.ts` 仍直接用 `updateSettings`：`app/usecases/viewinstance.usecase.ts:38-41, 58-62, 77-89`

## 8.3 不一致点 1：QuickInput create 的刷新策略与 AI 批量确认不一致

- QuickInput create：`scanFileByPath + notifyChange`：`platform/modals/QuickInputModal.tsx:518-523`
- AI 批量：只 `notifyChange`：`platform/modals/AiBatchConfirmModal.tsx:228-231, 262-271`
- Timer 新建：`scanFileByPath` 后再决定 start/notify：`features/timer/TimerService.ts:147-160`

## 8.4 不一致点 2：真正编辑 vs “伪编辑”共存

- 真编辑：`TaskRow` / `BlockItem` 走 `mode:'edit'`：`shared/ui/items/TaskRow.tsx:37-40`、`shared/ui/items/BlockItem.tsx:30-33`
- 伪编辑：`TimerRow` 打开的是 create 模式，只带 context：`features/timer/TimerRow.tsx:56-63`

## 8.5 不一致点 3：Modal 保存语义不一致

- `QuickInputModal`：save = 真提交
- `TemplateEditorModal`：save = 真提交
- `ModuleSettingsModal`：save = 等 100ms 后关闭，真实提交已在过程中发生：`features/settings/ModuleSettingsModal.tsx:208-217`

---

## 9. 五张清单（可直接带到第 2 轮）

## 9.1 写入口清单（压缩版）

### 记录/输入域
- `features/quickinput/registerCommands.ts`
- `platform/ObsidianModalPort.ts`
- `shared/ui/items/TaskRow.tsx`
- `shared/ui/items/BlockItem.tsx`
- `shared/ui/views/HeatmapView.tsx`
- `shared/ui/views/timelineInteraction.ts`
- `features/settings/LayoutRenderer.tsx`
- `features/timer/TimerViewView.tsx`
- `features/timer/TimerRow.tsx`
- `platform/modals/QuickInputModal.tsx`
- `platform/modals/AiBatchConfirmModal.tsx`
- `features/timer/TimerService.ts`
- `features/settings/LayoutRenderer.tsx`（`itemService.completeItem/updateItemTime`）

### 设置/配置域
- `features/settings/BlockManager.tsx`
- `features/settings/TemplateEditorModal.tsx`
- `features/settings/ThemeMatrixView.tsx`
- `features/settings/ThemeTreeNodeRow.tsx`
- `features/settings/useBatchOperations.ts`
- `features/settings/GeneralSettings.tsx`
- `features/settings/AiSettings.tsx`
- `features/settings/LayoutSettings.tsx`
- `features/settings/components/LayoutEditorPanel.tsx`
- `features/settings/ModuleSettingsModal.tsx`
- `features/settings/LayoutRenderer.tsx`
- `shared/ui/views/StatisticsView/StatisticsViewContainer.tsx`

## 9.2 UseCase 清单（压缩版）

- `BlocksUseCase`
- `ThemeUseCase`
- `LayoutUseCase`
- `ViewInstanceUseCase`
- `SettingsUseCase`
- `TimerUseCase`

**缺失项：** 没有对等的 `Input/Record` UseCase。

## 9.3 校验 / 补全清单（压缩版）

- 模板解析：`core/services/TemplateResolver.ts`
- QuickInput 默认值 / context 回填 / 时间联动：`app/ui/components/QuickInputEditor/QuickInputEditorContainer.tsx`
- 编辑态模板推断 / item → formData 回填：`platform/modals/QuickInputModal.tsx`
- 提交前 renderData normalize：`core/services/InputService.ts`
- Theme path 唯一性校验：`app/store/slices/theme.slice.ts`
- Block / Layout 默认值：`app/store/slices/blocks.slice.ts`、`app/store/slices/layout.slice.ts`
- ViewEditor normalize：`features/settings/*ViewEditor.tsx`
- Item 日期 normalize：`core/utils/normalize.ts`

## 9.4 副作用清单（压缩版）

- `SettingsRepository.update()` 持久化 + notify
- `initializeCore()` 中 settings 订阅带来的运行时副作用：timer widget / devConsole / categoryColors
- `InputService` / `ItemService` 写文件后的 `scanFileByPath + notifyChange`
- `QuickInputModal` / `AiBatchConfirmModal` / `TimerService` 自己加的 Notice、close、scan、notify
- `VaultWatcher` 带来的平台侧同步扫描

## 9.5 竞态热点清单（压缩版）

- QuickInput：edit 有 takeLatest，create 没有真正 abort
- AI 批量保存：无 takeLatest、无回滚、允许部分成功
- ModuleSettingsModal：编辑中即持久化，save 只是延时关闭
- ViewInstanceUseCase：依赖通用 `updateSettings`，无专门并发约束
- ItemService：scan/notify fire-and-forget
- Timer 新建任务链：create → scan → pick latest → start timer，缺事务边界
- StatisticsViewContainer：渲染副作用会反写全局设置

---

## 10. 这轮结束后，哪些事情已经“看清了”

我认为下面这些已经足够确定，可以作为第 2 轮输入：

### 已确定 1：quickinput 不能再继续长胖
因为它现在已经同时承担：
- 前台入口
- 表单默认值/时间联动
- 编辑态解析
- 提交控制
- 部分副作用协调

如果继续在这里堆逻辑，只会更难抽内核。

### 已确定 2：统一输入内核的第一接入点，确实应该先接 QuickInput
因为 QuickInput 已经是最多入口汇聚的前门：
- 命令
- 列表编辑
- Heatmap
- Timeline
- Layout 快捷录入
- Timer 新任务

先让它降级成“入口壳”，收益最大。

### 已确定 3：输入域的统一动作必须至少覆盖 create / update / refresh / feedback
因为当前 create/update 不是只差一个 save：
- create 有至少 3 种刷新策略
- update 有 `InputService.updateExistingRecord` 与 `ItemService.updateItemTime/completeItem` 两条子链
- feedback（Notice / closeModal）散在 UI

### 已确定 4：ViewInstance 域也需要收口，不然“统一编辑动作”做不稳
否则会出现：
- 输入域你收口了；
- 视图编辑域还继续到处 `useCases.viewInstance.*`；
- 最后“统一编辑动作”仍然不统一。

---

## 11. 我建议你下一轮（定规则）直接回答的 6 个问题

1. **输入域的新 facade 叫什么？**  
   例如：`recordInput.usecase.ts` / `inputKernel.usecase.ts` / `recordCommands.usecase.ts`

2. **QuickInputModal 以后是不是只保留“打开、收集、触发提交动作”三件事？**

3. **编辑已有记录时，模板推断逻辑要不要从 modal 抽成独立 resolver？**

4. **create 后刷新策略是否统一为：写文件 → scan 指定 path → notifyChange？**

5. **`ItemService.completeItem/updateItemTime` 算不算纳入统一记录动作模型？**  
   我倾向于算，因为它们本质上也是 record update。

6. **ViewInstance 要不要并回 `layout.usecase`，还是明确承认它是第二个 facade？**  
   现在文档和实现不一致，必须选边。

---

## 12. 最后一段判断

如果按你的总原则：

> **不要做第二个前台输入渠道。要做的是：新建统一输入内核，让 quickinput 先接进去。**

那基于这轮盘点，我的判断是：

### 你现在最值得确认的真实现状是

- **前台入口并不缺，已经太多了**；
- **缺的是输入域统一应用层**；
- **QuickInput 现在不是壳，而是半个内核**；
- **AI 批量、Timer 新建、Item 时间更新，都是未来必须并入统一动作模型的旁路**；
- **副作用和刷新策略还没收口**；
- **竞态控制只在少数链路做了一半**。

### 所以第 1 轮的结论不是“可以开始大改了”
而是：

> **现在已经可以开始第 2 轮：定规则。**  
> 因为“谁在写、写到哪、哪些地方绕过、哪些副作用在飞、哪些链路有竞态”——已经足够清楚了。

