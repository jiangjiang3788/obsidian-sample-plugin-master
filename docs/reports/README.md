# Implementation reports

All implementation and convergence reports shipped with the source package live in this directory.

The project root must stay clean: only the main `README.md` belongs there. Current design truth lives in the active documents under `docs/`; reports here are implementation history and verification notes.
