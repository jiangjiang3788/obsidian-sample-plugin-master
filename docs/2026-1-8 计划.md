| 步骤   | 优先级 | 目标（唯一推进）                                                                       | 修改类型     | 涉及文件（精确清单）                                                                                                                          | 工作量 | rg 验收（必须全为 0）                                                                                  |                                      |                                                                    |                            |
| ---- | --- | ------------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------- | --- | ---------------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------ | -------------------------- |
| P0-1 | P0  | **入口层清零：不再暴露/resolve AppStore**（先堵住“新增依赖”入口）                                   | 入口收敛     | `src/main.ts`、`src/features/aichat/AiChatModal.tsx`、`src/features/settings/index.ts`（类型依赖清理）                                        | S   | `rg "AppStore" src/main.ts src/features/aichat/AiChatModal.tsx src/features/settings/index.ts` |                                      |                                                                    |                            |
| P0-2 | P0  | **UseCases 去全局：createUseCases(store) + 注入 DI**（禁止任何 get/set 全局单例）              | 依赖显式化    | `src/app/usecases/index.ts`、`src/app/usecases/*`、`src/app/ServiceManager.ts`                                                        | M   | `rg "getAppStoreInstance                                                                       | getAppStore                          | get/setAppStoreInstance                                            | AppStoreInstance" src/app` |
| P0-3 | P0  | **ServiceManager 不再 resolve(AppStore)**（拔掉最后的 DI 依赖）                           | 架构收敛     | `src/app/ServiceManager.ts`、（可能）`src/core/di/setupCore.ts`                                                                          | S/M | `rg "resolve\(AppStore\)                                                                       | container\.resolve\(AppStore\)" src` |                                                                    |                            |
| P0-4 | P0  | **ThemeMatrix service 收口：禁止 writeOps/第二写入口**（写入口唯一化示范）                         | 真源收敛/去重  | `src/core/theme-matrix/ThemeMatrixService.ts`、`ThemeScanService.ts`、`src/app/usecases/theme.usecase.ts`                             | M   | `rg "writeOps                                                                                  | setWriteOps                          | \bWriteOps\b" src/core/theme-matrix src/app/usecases`              |                            |
| P0-5 | P0  | **命令注册去快照：commands 随 settings/blocks 变更更新（唯一读：zustand selector，唯一写：useCases）** | 风险控制/单向流 | `src/features/quickinput/registerCommands.ts`、`src/features/aiinput/registerCommands.ts`、（可能）`src/app/usecases/settings.usecase.ts` | M   | `rg "AppStore                                                                                  | useAppStore                          | getAppStoreInstance" src/features/quickinput src/features/aiinput` |                            |
| P0-6 | P0  | **删除 AppStore 文件本体 + 一次性扫尾**（此时已经没有任何入口/DI/usecase依赖）                          | 删除冗余     | `src/app/AppStore.ts`、所有残余引用点                                                                                                       | M   | `rg "\\bAppStore\\b" src`（只允许剩下类型名注释/文档；最好为 0）                                                 |                                      |                                                                    |                            |
| P1-1 | P1  | **工具/类型去重**（ThemeStatus/common 等只保留一个真源）                                       | 统一语义/去重  | `src/shared/types/*`、`src/core/types/*`、相关引用                                                                                        | S/M | `rg "enum ThemeStatus                                                                          | type ThemeStatus                     | ThemeStatus =" src/shared src/core`（只留一处）                          |                            |
| P1-2 | P1  | **CI/脚本防回流固化**（迁移后永不复发）                                                        | 约束固化     | `scripts/*`、`src/app/ARCH_CONSTRAINTS.md`                                                                                           | S   | `rg "@features/                                                                                | AppStore                             | /slices/" src/app/store`（按你规则设白名单/黑名单）                             |                            |
