| 阶段     | 优先级 | 修改类型                             |  进度 | 原因说明                           | 涉及文件                                                                | 预估时间 | 危险性 | 验收                                                           |
| ------ | --- | -------------------------------- | --: | ------------------------------ | ------------------------------------------------------------------- | ---- | --- | ------------------------------------------------------------ |
| Phase0 | P0  | **配置卫生（alias/tsconfig/vite 一致）** | 0/3 | 最快降低“莫名其妙路径/IDE报错/引入错层”噪音，减少堵点 | `tsconfig.json` / `vite.config.ts`（清理不存在 alias；必要时启用 tsconfigPaths） | S-M  | ⚠️  | alias 只保留真实存在目录；TS/IDE/构建解析一致；`npm run typecheck` 通过         |
| Phase0 | P0  | **工程健康：gate 输出统一（formatter）**    | 0/2 | 输出统一后你排错速度会快一倍；也更适合 CI         | `scripts/*gate*.mjs`（统一 formatter）/ `package.json` gate 串联输出        | S    | ⚠️  | gate 输出统一：规则名/文件/行/建议；本地与 CI 一致                              |
| Phase0 | P0  | **门禁可执行化（让 check 至少可跑）**         | 0/5 | 先让门禁“能跑”，否则再多规则都是噪音            | `package.json`（check串联）/ `vite.config.ts`（unused import 修复）         | S-M  | 🔥  | `npm run check` 至少包含 `lint + typecheck + gate` 并稳定可跑；失败能定位到行 |
| Phase0 | P1  | 架构门禁/止血（Obsidian API 泄漏）         | 0/5 | 根因止血但量大；先留到小项之后再推进             | `.eslintrc-architecture.js` / `scripts/*gate*.mjs` / allowlist      | M    | 💥  | 除 `src/platform/**`（+allowlist）外，`from 'obsidian'` 失败并定位     |
| Phase0 | P1  | 平台边界（补 ports + adapter）          | 0/4 | 为 TSX 迁移铺路，但属于结构性工作            | `src/core/ports/**` + `src/platform/**`                             | L    | 🔥  | core/features 不直接依赖 obsidian 类型；至少 1 条真实调用链跑通                |
| Phase0 | P1  | 迁移/止血：TSX 直连 obsidian 清零         | 0/6 | 最大项，留到门禁/配置稳定后做，否则越迁越堵         | 你列的 TSX 清单 + 全仓 `.tsx` 命中点                                          | XL   | 💥  | `.tsx` 中 `from 'obsidian'` 命中=0（或仅 platform adapter）         |
| Phase0 | P2  | 生命周期收口：统一 dispose + 卸载安全         | 0/4 | 很重要但会牵扯面较大；等结构稳定再收             | `DataStore`、runtime、订阅/timer 命中点                                    | M    | 💥  | unload/reload 后无遗留订阅/定时器；无卸载后写文件                             |
| Phase0 | P2  | Modal Promise 不悬挂（关闭也 resolve）   | 0/3 | 小但需要定位到具体 modal，放在 P2 也可以      | 对应 modal 文件                                                         | S    | 🔥  | 点 X/遮罩关闭不悬挂；不会 await 卡死                                      |
| Phase0 | P2  | 共享层去平台化（Notice 等）                | 0/3 | 需要 ports 基础，放到 ports 后面更顺      | `errorHandler.ts` / `UiPort` / `platform/ui/*`                      | M    | 🔥  | shared/core 无 obsidian import；notice 经 `UiPort`              |
| Phase0 | P2  | 最小测试框架（3~6 smoke）                | 0/5 | 你现在主要诉求是先不堵；测试可以在“门禁跑通后”再上     | 新增 `test/` + `jest/vitest` config                                   | M    | 🔥  | `test:unit` 可跑且稳定；3~6 个 smoke tests                          |
| Phase1 | P0  | 边界简化（禁深导入硬闸）                     | 2/3 | 如果你已经做到 2/3，就继续收尾              | `shared/public.ts` / eslint 规则 / 引用迁移                               | M    | 🔥  | allowlist 外深导入 gate fail；命中清零                                |
| Phase1 | P1  | 平台事件迁移（vault/workspace）          | 0/4 | 结构性迁移，慢慢做                      | `VaultWatcher` → `platform/events`                                  | M-L  | 🔥  | feature 不直触 vault/workspace；订阅统一经 EventsPort 且可 dispose      |
| Phase1 | P1  | DI 简化（resolve 收敛）                | 1/4 | 中等工程量，后置                       | `buildRuntime` + resolve 命中点                                        | L    | ⚠️  | 非 bootstrap resolve 降到阈值；其余注入                                |
| Phase1 | P1  | 状态简化（selectors 普及）               | 1/5 | 长线优化，后置                        | `selectors/*` + 页面迁移                                                | L    | ⚠️  | 覆盖≥60%；订阅优化可做                                                |
| Phase1 | P1  | 性能边界（索引/检索/扫描限流）                 | 0/4 | 需要测量与链路梳理，后置                   | Retrieval/DataStore/AI context                                      | M    | 🔥  | limit 收紧；索引异步/分段；引用只记录实际使用项                                  |
| Phase1 | P1  | 拆巨石（容器/视图分离）                     | 0/6 | 大工作，后置                         | StatisticsView/QuickInputEditor 等                                   | XL   | ⚠️  | <250 LOC 或拆分；View 纯渲染                                        |
| Phase1 | P2  | 竞态/取消统一（takeLatest/Abort）        | 0/4 | 后置                             | AiHttpClient + hooks/usecase                                        | M-L  | 🔥  | 关键链路可取消；unmount 不 setState                                   |
| Phase1 | P2  | 发布元数据一致性                         | 0/2 | 小项但不是你现在最堵的点                   | `manifest.json`/`package.json`                                      | S    | ⚠️  | version/minAppVersion 等一致                                    |
| Phase2 | P0  | Effect 系统                        | 0/4 | 长线                             | `app/effects`                                                       | XL   | ⚠️  | 骨架+两条链落地                                                     |
| Phase3 | P1  | DataStore 拆分                     | 0/4 | 长线                             | `DataStore`                                                         | XL   | ⚠️  | Repo/Cache/Indexer 拆分，回归通过                                   |
| Phase4 | P1  | 工程收尾文档化                          | 0/2 | 最后做                            | `docs/`/README                                                      | M    | 🙂  | 新人按文档可跑 check/gate                                           |
