| 步骤 | 优先级 | 当前进度 / 预防 | 目标 | 修改类型 | 涉及文件（精确清单） | 产出 / 验收重点 |
| --- | --- | --- | --- | --- | --- | --- |
| S0 | P0 | ✅ 已完成 ｜ 预防：清理 index.ts 中遗留注释，避免误导后续导入 | **先修编译断点**（否则验收跑不起来） | 修 bug | `src/core/theme-matrix/index.ts`、`src/features/timer/TimerStateService.ts`、`src/features/timer/TimerRow.tsx` | `npm run build` 能跑；不再引用不存在模块 |
| S1 | P0 | ✅ 已完成 ｜ 预防：`core/config` 只允许纯配置对象，不得混入 feature/service 逻辑 | **边界修复：store 不得 import features registry** | 架构收敛 | `src/app/store/slices/viewInstance.slice.ts`、`src/core/config/viewConfigs.ts` | `src/app/store/**` 内不出现 `@features/` import |
| S2 | P0 | ✅ 已完成 ｜ 预防：SettingsRepository → Store 同步禁止反向 update，避免循环 | **迁移垫片：SettingsRepository 增加 subscribe + 同步 AppStore/Zustand** | 架构垫片 | `src/core/services/SettingsRepository.ts`、`src/app/ServiceManager.ts`、`src/app/AppStore.ts` | 旧 UI（AppStore）和新 UI（Zustand）**实时看到同一份 settings** |
| S3 | P0 | ✅ 已完成 ｜ 预防：UI 中“写 settings”只能走 UseCases，不得直接 set store | **从点开始：迁最简单的 Settings UI 到 Zustand** | UI 切换 | `src/features/settings/AiSettings.tsx`、`BlockManager.tsx`、`HeatmapViewEditor.tsx`、`GeneralSettings.tsx`、`ModuleSettingsModal.tsx` | 文件内不再 `import { useStore } from '@/app/AppStore'` |
| S4 | P0 | 🟡 部分完成 ｜ 预防：SettingsTab 仍是旧入口，需在 S10.1 彻底去 props 化 | **切核心树：LayoutSettings 全量改走 Zustand + UseCases**（删 useSettingsManager） | UI + 删除冗余 | `LayoutSettings.tsx`、`SettingsTab.tsx`、`useSettingsManager.ts` | Layout 树增删改/拖拽可用；`useSettingsManager` 删除 |
| S5 | P0 | ✅ 已完成（Facade 口径）｜ 预防：严禁新增第二写入口（slice / legacy store） | **真源唯一：ViewInstance CRUD / reorder 只保留一套** | API 去重 | `layout.usecase.ts`、`layout.slice.ts`、`viewInstance.slice.ts`、`AppStore.ts` | **唯一写入口：`useCases.layout.*`；legacy store 已禁用；group 保持禁用** |
| S6.0 | P0 | ⛔ 未开始 ｜ 预防：先定“写入口 & UI 临时态边界”，否则 UI/数据会互相污染 | **ThemeMatrix 迁移前置：定义唯一写入口与订阅策略** | 设计收口 | `src/app/usecases/theme.usecase.ts`（新增/骨架）、`ARCH_CONSTRAINTS.md` | 明确：ThemeMatrix 写走 `useCases.theme.*`，读走 Zustand |
| S6.1 | P0 | ⛔ 未开始 ｜ 预防：禁止“只改 service 不改 UI” | **ThemeMatrix UI 去 AppStore，接 Zustand + useCases** | UI 迁移 | `ThemeMatrix.tsx`、`ThemeTable.tsx`、`ThemeTreeNodeRow.tsx`、`InputSettings.tsx`、`props.types.ts` | UI 可编辑/保存；grep 无 `AppStore/useStore` |
| S6.2 | P0 | ⛔ 未开始 ｜ 预防：避免 settings 变化触发全量 rerender | **ThemeMatrix 服务层去 AppStore，改订阅 Zustand / repo** | 服务流收敛 | `ThemeMatrixService.ts`、`ThemeScanService.ts`、`ServiceManager.ts` | theme 修改后 UI 自动更新，不靠 AppStore.subscribe |
| S7.0 | P0 | ⛔ 未开始 ｜ 预防：FeatureLoader 不得再要求 appStore 必选 | **QuickInput/AiInput 前置：FeatureLoader 接口去 appStore** | 依赖解耦 | `FeatureLoader.ts`、`ServiceManager.ts` | features 可加载；至少一个命令可注册 |
| S7.1 | P0 | ⛔ 未开始 ｜ 预防：命令注册阶段不要直接读 store 实例 | **QuickInput 全链路去 AppStore（含命令/UI）** | 功能块迁移 | `src/features/quickinput/*` | 弹窗可用；设置读 Zustand、写 useCases |
| S7.2 | P0 | ⛔ 未开始 ｜ 预防：避免 AI 流程偷偷依赖 appStore.getSettings | **AiInput 全链路去 AppStore（含命令/UI）** | 功能块迁移 | `src/features/aiinput/*` | AI 输入流程可跑；无 appStore 依赖 |
| S8.1 | P0 | ⛔ 未开始 ｜ 预防：只订阅 layout/viewInstances selector，避免全量刷新 | **RendererService 改订阅 Zustand** | 服务流收敛 | `RendererService.ts`、`ServiceManager.ts` | layout 改动后 dashboard 自动刷新 |
| S8.2 | P0 | ⛔ 未开始 ｜ 预防：embedder 禁止直接读 appStore.getSettings | **CodeblockEmbedder 改读 Zustand + useCases** | 服务流收敛 | `CodeblockEmbedder.ts` | 行为正确；无 AppStore 访问 |
| S9.1 | P1 | ⛔ 未开始 ｜ 预防：Timer 高频 tick 不得写入 settings | **Timer 进 Zustand（TimerSlice + useCases）** | 大块迁移 | `timer.slice.ts`、`timer.usecase.ts`、`features/timer/*` | TimerView 不依赖 AppStore；状态正常 |
| S9.2 | P1 | ⛔ 未开始 ｜ 预防：Timer 服务注入不得再 resolve AppStore | **Timer 服务层断 AppStore** | 架构收敛 | `ServiceManager.ts` | Timer 运行不需 AppStore |
| S10.1 | P1 | 🟡 未完成 ｜ 预防：SettingsTab 不得再通过 props 传 store | **拔 AppStore：SettingsTab 入口去 props 化** | 入口收敛 | `SettingsTab.tsx`、`AppStoreContext.tsx` | SettingsTab 不 import AppStore |
| S10.2 | P1 | ⛔ 未开始 ｜ 预防：services 必须能在无 AppStore 下工作 | **ServiceManager 不再 resolve(AppStore)** | 架构收敛 | `ServiceManager.ts`、`setupCore.ts` | `container.resolve(AppStore)` 归零 |
| S10.3 | P1 | ⛔ 未开始 ｜ 预防：删文件前先保证主链路可跑 | **删除 AppStore 与 legacy store** | 删除冗余 | `AppStore.ts`、`features/settings/*Store.ts` | AppStore 可删；构建通过 |
| S11.1 | P1 | ⛔ 未开始 ｜ 预防：禁止隐式全局单例 | **UseCases 去全局：createUseCases(store)** | 依赖显式化 | `usecases/*`、`usecases/index.ts` | 无 get/setAppStoreInstance |
| S11.2 | P1 | ⛔ 未开始 ｜ 预防：commands/services 也要能拿 useCases | **非 React 场景 useCases 注入** | 依赖显式化 | `ServiceManager.ts` / DI | command 注册正常 |
| S12.1 | P1 | ⛔ 未开始 ｜ 预防：ID/array utils 禁止多来源 | **清理重复工具/ID** | 删除冗余 | `utils/array.ts`、ID 生成器 | 唯一工具入口 |
| S12.2 | P1 | ⛔ 未开始 ｜ 预防：防回流 | **唯一入口 grep/CI 约束** | 约束 | scripts / CI | features 不可 import slice/AppStore |
