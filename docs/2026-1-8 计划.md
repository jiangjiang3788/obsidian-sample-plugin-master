| 步骤  | 优先级 | 当前进度 / 预防                                                     | 目标                                                                        | 修改类型         | 涉及文件（精确清单）                                                                                                              | 工作量 | 原因（为什么这一步现在要做）                               | 产出 / 验收重点                                                                             |
| --- | --- | ------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------- | --- | -------------------------------------------- | ------------------------------------------------------------------------------------- |
| S0  | P0  | ⛔ 未开始｜预防：两套 ESLint 规则并行会互相打架，导致约束“看似存在但没生效”                   | **Lint 唯一真源：整合 `.eslintrc-architecture.js` 到 `.eslintrc.cjs` 并统一 rule 名** | 约束收敛         | `.eslintrc.cjs`、`.eslintrc-architecture.js`                                                                             | S   | 迁移期必须防回流，否则后期排查成本爆炸                          | `npm run lint` 覆盖所有架构规则且稳定；features import slices/AppStore 立刻报错                       |
| S1  | P0  | ⛔ 未开始｜预防：你担心“删 AppStore 后更难查”，必须先有“谁在写”可观测性                   | **低成本可观测性：Zustand 写入 actionName + diff（dev only）**                        | 基础设施         | `src/app/store/store.ts`（或真实 store 文件）、`src/app/usecases/*`                                                             | S   | 不加观测性，你每迁一步都像盲飞；加了以后 bug 定位成本显著降低            | dev 环境能看到每次 useCases 写入：useCase 名 + patch/diff；能够定位写入来源（UI/command/service）           |
| S2  | P0  | ⛔ 未开始｜预防：迁移期最怕“以为迁完但旧入口还在用”                                   | **断路测试 A（Circuit Breaker）：AppStoreContext 类型金丝雀 + useAppStore 打印堆栈**      | 风险控制         | `src/app/AppStoreContext.tsx`                                                                                           | S   | 用最小改动把“僵尸依赖”全部暴露出来；比删文件安全，排查更精确              | 改完后：build + 手动打开所有面板；控制台报错堆栈能定位残余 useAppStore 调用点，并逐个清零                               |
| S3  | P0  | ⛔ 未开始｜预防：隐性 DI 依赖最危险（白屏/启动失败）                                 | **拔管测试 B：ServiceManager 暂时不注入 AppStore（启动探测）**                            | 风险控制         | `src/app/ServiceManager.ts`、（如有）`src/app/setupCore.ts`                                                                  | S   | 用“故意断供”快速确认核心链路是否已经解耦；比删 AppStore 更便宜、更早发现问题 | 应用能启动且主链路可跑（至少能进入 dashboard/打开 settings）；若白屏，必须修复残余依赖再继续                              |
| S4  | P0  | 🟡 部分完成｜预防：Theme 真源不收敛，删 AppStore 后会出现“重启不一致”的难查 bug          | **Theme 真源收敛：Theme 状态以 settings 为唯一真源，禁止内存 Map 成事实来源**                    | 真源收敛 / 修 bug | `src/core/theme-matrix/ThemeMatrixService.ts`                                                                           | M   | 你已明确这是 P0 极高问题：archive/activate 不持久/不一致      | 归档/激活主题后刷新/重启仍一致；UI 展示与 settings 一致；内存 Map 只能作为派生缓存                                   |
| S5  | P0  | ⛔ 未开始｜预防：inactive/archived 语义分裂会让 slice/usecase/service 永远对不齐 | **统一语义：ThemeStatus 单一枚举 + 删除重复 type 入口**                                  | 统一语义 / 去重    | `src/shared/types/common.ts`、`src/core/types/common.ts`、`src/app/store/slices/theme.slice.ts`、`src/core/theme-matrix/*` | M   | 类型不统一会导致 any 绕过、后期很难收敛；越晚越贵                  | 全局只保留一套 ThemeStatus 定义；grep 无重复 status 定义；无 any 绕过                                    |
| S6  | P0  | ⛔ 未开始｜预防：ThemeMatrix 大树订阅全量 settings 会拖垮性能，并掩盖真实 bug          | **ThemeMatrix UI 最小订阅 + 单向流：读 Zustand selector，写 useCases.theme.***       | UI 迁移 + 性能收敛 | `src/features/settings/ThemeMatrix.tsx`、`ThemeTable.tsx`、`ThemeTreeNodeRow.tsx`、`InputSettings.tsx`、`props.types.ts`    | M   | 这是你“单向服务/数据/事件流”的核心示范模块                      | ThemeMatrix 不再 import AppStore/useStore；只订阅必要字段；编辑/保存可用；非相关 settings 改动不触发全量 rerender |
| S7  | P0  | ⛔ 未开始｜预防：core/service 订阅旧 store 会阻塞 S10 并造成双订阅 bug            | **ThemeMatrix 服务流收敛：去 AppStore.subscribe，订阅 Zustand/repo**                | 服务流收敛        | `src/core/theme-matrix/ThemeMatrixService.ts`、`ThemeScanService.ts`、`src/app/ServiceManager.ts`                         | M   | 没有这一步你无法真正删除 AppStore；服务会成为隐性依赖              | theme 修改后 UI 自动更新；不依赖 AppStore；grep 无 AppStore.subscribe                              |
| S8  | P0  | 🟡 部分完成｜预防：LayoutSettings 仍混入旧 manager 会造成“第二写入口”回流           | **LayoutSettings 全量走 Zustand + useCases（删 useSettingsManager）**           | UI + 删除冗余    | `LayoutSettings.tsx`、`SettingsTab.tsx`、`useSettingsManager.ts`                                                          | M   | 你已在 S4 做了一半，这一步必须收口，否则 S10 会有残余 props 链路     | Layout 树增删改拖拽可用；useSettingsManager 删除；Layout 修改能触发 renderer 更新                        |
| S9  | P0  | ⛔ 未开始｜预防：Service/Renderer/Embedder 读 AppStore 会让“删文件”变成地雷阵    | **关键服务读路径改订阅 Zustand selector（最小集）**                                      | 服务流收敛        | `RendererService.ts`、`CodeblockEmbedder.ts`、`src/app/ServiceManager.ts`                                                 | M   | 这是删 AppStore 的最大实际阻塞点之一                      | layout 改动 dashboard 自动刷新；embedder 无 appStore.getSettings；服务启动无 AppStore               |
| S10 | P0  | ⛔ 未开始｜预防：FeatureLoader 要求 appStore 必选会导致“删 AppStore 后全功能断”    | **FeatureLoader 接口去 appStore；QuickInput/AiInput 全链路去 AppStore**           | 功能块迁移 / 依赖解耦 | `FeatureLoader.ts`、`ServiceManager.ts`、`src/features/quickinput/*`、`src/features/aiinput/*`                             | M   | commands/features 是隐藏依赖重灾区；必须在删之前清干净         | features 可加载；QuickInput 弹窗可用；AiInput 可跑；grep 无 appStore 依赖                            |
| S11 | P1  | ⛔ 未开始｜预防：Timer 高频状态不能污染 settings（会导致无意义写入、性能差、状态错）            | **Timer 进 Zustand：TimerSlice + useCases.timer（tick 不写 settings）**         | 大块迁移         | `timer.slice.ts`、`timer.usecase.ts`、`src/features/timer/*`、`ServiceManager.ts`                                          | M   | Timer 属于高频域，必须明确“domain state vs UI state”边界 | TimerView 不依赖 AppStore；tick 正常；settings 不被写入；Timer 服务不 resolve AppStore               |
| S12 | P1  | 🟡 未完成｜预防：SettingsTab props 传 store 是旧入口残余，会阻塞彻底删除            | **SettingsTab 入口去 props 化（彻底断开旧入口）**                                      | 入口收敛         | `SettingsTab.tsx`、`AppStoreContext.tsx`                                                                                 | S   | 这是删 AppStore 前必须做的“最后一根 props 管道”            | SettingsTab 不 import AppStore；不再通过 props 传 store；UI 仍可用                               |
| S13 | P1  | ⛔ 未开始｜预防：删 AppStore 前必须保证 DI/服务不再 resolve(AppStore)           | **ServiceManager 不再 resolve(AppStore)**                                   | 架构收敛         | `src/app/ServiceManager.ts`、`src/app/setupCore.ts`                                                                      | M   | 这一步完成才有资格做删除；否则删文件会直接炸                       | `container.resolve(AppStore)` 归零；服务启动与运行无 AppStore                                    |
| S14 | P1  | ⛔ 未开始｜预防：删文件前先跑断路测试/拔管测试确保“零依赖”                               | **删除 AppStore 与 legacy store（最终拆除）**                                      | 删除冗余         | `src/app/AppStore.ts`、`src/features/settings/*Store.ts`、所有 legacy 引用点                                                   | M   | 前面做的是“让它可以被删”，这里才是“真的删”                      | build/lint 通过；主链路可跑；grep 无 AppStore；断路测试不再报任何堆栈                                       |
| S15 | P1  | ⛔ 未开始｜预防：迁移后复用率不升反降、工具多来源会回到旧问题                               | **清理重复工具/ID + CI 防回流固化**                                                  | 删除冗余 / 约束    | `utils/array.ts`、ID 生成器、`scripts/`、`ARCH_CONSTRAINTS.md`                                                                | S   | 防止“迁移完成后又长回去”；保证长期一致性                        | 唯一工具入口；CI 强制 features 不可 import slices/AppStore；core 不可 import features               |
