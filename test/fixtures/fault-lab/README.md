# Think OS 故障实验室固定样本

本目录只保存**可重复、不可变的输入样本**。测试需要制造“磁盘写入失败、并发修改、超时”等动态故障时，使用 `test/support/faultLab.ts` 的故障注入器，而不是修改业务源码。

原则：

- 样本名称和测试输出使用中文；
- 每个样本必须登记在 `test/system/fault-lab.json`；
- 样本只代表输入，不代表测试已经执行通过；
- 超大数据通过确定性生成器产生，避免把数 MB 垃圾文本塞进仓库。
