# E2E / 数据安全测试 Vault 规划

当前仓库已有 `simple/` 冒烟 Vault。后续按测试体系补齐时，建议逐步加入真实、可版本化的样本：

- `empty/`：空 Vault / 首次安装；
- `simple/`：最小正常数据；
- `legacy-<version>/`：从真实旧版本固定下来的升级样本；
- `corrupted/`：可控损坏记录、未知字段、半写入数据；
- `large/`：用于启动、索引、查询性能的可重复数据集。

不要手工“猜”旧版本数据结构。Legacy fixture 应从真实发布版本生成并固定下来。
